# Server Configuration
PORT=3001
NODE_ENV=development

# Airtable Configuration (REQUIRED for server)
# These are used by the Express server - keep them secret!
AIRTABLE_BASE_ID=your_base_id_here
AIRTABLE_API_KEY=your_api_key_here

# Frontend Configuration
# For local development with Express server
VITE_API_ENDPOINT=http://localhost:3001/api/submit-quiz

# For production, set this to your deployed server URL
# VITE_API_ENDPOINT=https://your-domain.com/api/submit-quiz
# FRONTEND_URL=https://your-domain.com

# Optional: Direct API access (development only - NOT RECOMMENDED)
# VITE_AIRTABLE_BASE_ID=your_base_id_here
# VITE_AIRTABLE_API_KEY=your_api_key_here